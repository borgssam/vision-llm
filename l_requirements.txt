# =============================================================================
# Vision-LLM 실습 환경 requirements
# 대상: NVIDIA Jetson (JetPack 6.x / CUDA 12.6 / aarch64 / Python 3.10)
#
# [설치 방법] 반드시 --no-deps 로 설치하세요.
#
#   python3 -m venv --system-site-packages .venv
#   source .venv/bin/activate
#   python -m pip install --upgrade pip setuptools wheel
#   pip install --no-deps -r requirements.txt
#
# --no-deps 를 쓰는 이유:
#   pip 가 의존성을 자동 해결하면 JetPack 이 제공하는 aarch64/CUDA 빌드
#   (opencv, tensorrt, numpy 등)를 PyPI 의 일반 빌드로 덮어써서 GPU/카메라가
#   동작하지 않게 됩니다. 각 노트북(00~04)의 설치 절차와 동일한 정책입니다.
#
# [시스템 제공 패키지 - 여기에 넣지 않음]
#   cv2(OpenCV, GStreamer 빌드), tensorrt, Jetson.GPIO, pandas, scipy,
#   requests, psutil, PyYAML, matplotlib
#   → `--system-site-packages` venv 를 통해 그대로 사용합니다.
#
# [apt 로 별도 설치 필요]
#   sudo apt install python3-venv v4l-utils
# =============================================================================


# --- 00. Jupyter 실습 환경 ---------------------------------------------------
jupyter==1.1.1
ipykernel==7.3.0
ipywidgets==8.1.8
jupyterlab_widgets==3.0.16
widgetsnbextension==4.0.15
matplotlib-inline<0.2

# --- 02. Computer Vision -----------------------------------------------------
# ultralytics 요구사항(numpy>=1.23.0)에 맞춘 핀. JetPack 기본은 1.21.5 입니다.
numpy==1.23.0
scikit-image==0.19.3
imageio==2.19.3
tifffile==2022.5.4
PyWavelets==1.3.0
networkx==2.8.8

# 웹캠 스트리밍 헬퍼 (imutils.video.VideoStream)
imutils==0.5.4

# MediaPipe (Hand/Face/Pose Landmarker)
mediapipe==0.10.14
absl-py==2.1.0
flatbuffers==24.3.25
sounddevice==0.4.7

# --- 03. Deep Learning & GPU -------------------------------------------------
# torch / torchvision 은 반드시 Jetson 전용 인덱스의 CUDA 빌드를 써야 합니다.
# PyPI 에도 동일 버전이 있어 pip 가 잘못된 휠을 고를 수 있으므로,
# `import torch; torch.cuda.is_available()` 가 False 면 아래 명령으로 재설치하세요.
#
#   pip install --no-deps --index-url https://pypi.jetson-ai-lab.io/jp6/cu126 \
#       torch==2.8.0 torchvision==0.23.0
--extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126
torch==2.8.0
torchvision==0.23.0
sympy>=1.13.3
Pillow==10.4.0

# 모델 구조 시각화
aggdraw==1.3.19
visualtorch==0.2.3

# CuPy (GPU 가속 배열 연산)
fastrlock==0.8.3
cupy-cuda12x==12.3.0

# --- 04. Object Detection (YOLO) ---------------------------------------------
ultralytics
typing_extensions>=4.10.0,<5
filelock>=3.16.1,<4
# ultralytics 런타임이 실제로 import 하는 최소 의존성
tqdm>=4.64.0
py-cpuinfo
ultralytics-thop>=2.0.0

# ONNX / TensorRT 내보내기
onnx==1.16.0
onnxruntime==1.23.2
onnxslim==0.1.95
ml_dtypes==0.5.4
coloredlogs==15.0.1
humanfriendly==10.0
protobuf==3.20.3
